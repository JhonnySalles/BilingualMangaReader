<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primary"
    android:fitsSystemWindows="true"
    tools:context=".view.ui.manga_detail.MangaDetailActivity">

    <FrameLayout
        android:id="@+id/root_frame_manga_detail"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/on_primary"
        android:fitsSystemWindows="true" />

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar_manga_detail"
        android:paddingTop="@dimen/default_navigator_header_padding"
        android:layout_width="match_parent"
        android:layout_height="@dimen/default_navigator_header_size"
        android:background="@color/transparent"
        android:gravity="start|center"
        app:popupTheme="@style/MyDarkToolbarStyle"
        app:theme="@style/ToolbarTheme"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>